# Findings

> Auto-generated by ShipLoop Phase 2 (EXPLORE). Feeds into the plan.

## Project Type

**Detected:** Web app (language learning) + Flask API + Railway deployment  
**Stack:** Python 3 / Flask 3.1, vanilla JS modules in `static/js/`, monolithic `index.html` (~9.5k lines, inline CSS)  
**Package manager:** pip (`requirements.txt`)  
**Verification:** Browser (primary), HTTP/API smoke tests, manual UX review; **no automated test suite** found

## What's Working Well

- Clear README and `docs/` after recent cleanup; GitHub sync on personal account
- Modular frontend split across `static/js/ui/*` (levels, lesson, practice, localization admin)
- i18n infrastructure exists: `i18n.js`, `window.t()`, DB-backed `/api/localization/<lang>`, AI fill for missing keys
- Design tokens in CSS variables (`--accent`, `--card`, etc.) — good base for a cohesive UI
- Auth, levels, practice, marketplace, and admin localization UI are implemented (not greenfield)
- Recent commit reduced repo noise; backup branch on GitHub

## Independent Observations

- **Monolith risk:** `index.html` (~9.5k lines) + `app.py` (~8.7k lines) — hard to maintain; UI/CSS/JS not separated into assets files (0 `.css` files)
- **Mixed locales:** HTML `lang="de"` but many English fallbacks; German hardcoded in `levels.js` (lock dialogs, messages) bypassing i18n
- **Unfinished UX:** TODOs in `index.html` (show all words, account deletion); product feels MVP
- **Local env gap:** No `.env` in repo (expected); no `venv` / Flask not installed in current shell — dev must be bootstrapped before verify loop
- **Security notes (fix carefully):** Test password `password123` in docs/scripts; SHA256 password hashing in some DB bootstrap paths; admin check commented out in `app.py` (TODO re-enable)
- **137 `data-i18n` hooks** in HTML vs many dynamic strings built via `innerHTML` in JS — translation gaps likely
- **No pytest/unittest** — regression risk; Phase 4 relies on browser verification
- **`.env.example`** contains placeholder secrets (example only — must not copy literally to production)

## Potential Issues Found

- **[HIGH]** Inconsistent i18n — hardcoded German/English strings in JS; missing DB translations → unfinished feel
- **[HIGH]** Visual inconsistency — large inline stylesheet, mixed patterns; “not clean” aligns with monolithic HTML + ad-hoc `innerHTML`
- **[HIGH]** Feature reliability unknown without running app — user reports not all features bug-free; core flows need systematic test
- **[MEDIUM]** No local dev environment ready (missing venv, `.env`, dependencies)
- **[MEDIUM]** Unfinished features exposed (TODOs) or dead UI paths
- **[MEDIUM]** `app.py` size/complexity — bugs and regressions harder to isolate
- **[LOW]** Verbose `console.log` in levels flow
- **[LOW]** Admin gate disabled for testing

## Code Quality Assessment

| Area | Assessment |
|------|------------|
| Structure | Backend services exist under `server/` but `app.py` still central; frontend modular JS but single HTML shell |
| Naming | Generally consistent in JS modules |
| Tests | None detected |
| Docs | Good post-cleanup README + docs |
| Security | Must harden before paid users; no committed live secrets observed in tracked files |

## Recommended Verification Approach

1. Bootstrap: `python3 -m venv venv && pip install -r requirements.txt`; restore `.env` from archive backup (not committed)
2. Run `python3 run_local.py` → http://localhost:5001
3. Browser test matrix: login → pick language → levels grid → start lesson → practice → settings → marketplace (smoke)
4. i18n check: switch UI locale DE/EN; note raw keys or hardcoded strings
5. Optional: `GET /api/localization/de` and compare missing keys vs UI surfaces
6. Before Railway: security review on auth/admin routes; never commit `.env`
