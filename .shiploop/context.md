# Project Context

> Generated by ShipLoop during Phase 1 (LISTEN).
> This file is the single source of truth for what the user wants.

## Project

**Name:** ProjectSiluma
**Description:** Language-learning web app (Flask) with custom levels, practice modes, and localization.
**Who it's for:** Owner first (personal use when stable); later other learners at a symbolic price.
**Repository:** `/Users/Air/Library/Mobile Documents/com~apple~CloudDocs/03_Projects/ProjectSiluma`

## Project Type

**Primary:** Web app (language learning) — *confirmed by exploration*
**Secondary:** API backend (Flask) + deployed workers (Railway)
**Language:** Python (backend), JavaScript (frontend in `static/`)
**Framework:** Flask
**Package Manager:** pip (`requirements.txt`)

## Tech Stack

- Python 3 + Flask (`app.py`, `server/`)
- Frontend: static assets (`static/`)
- Database: PostgreSQL (production) / local per docs
- Deployment: Railway (`railway_startup.py`, `cleanup_service.py`)
- GitHub: `linuspohlmann-byte/projectsiluma` (personal account)

## User's Priorities

Ranked by importance (1 = most important):

1. **Reliability — app runs cleanly** — stable behavior, few bugs/crashes; trustworthy for daily learning use.
2. **UX / UI — extremely user-friendly** — learning app people *want* to use; polish, clarity, delight (not just functional).
3. **Clean foundation (“Harnisch”)** — maintainable codebase, clear structure, sensible patterns; easy to evolve toward paid/symbolic offering later.

## Definition of Done

What "done" looks like for this run (user-stated):

- UI feels **extremely user-friendly** for a learning app — intuitive flows, pleasant to use repeatedly.
- App **runs cleanly** (no obvious broken paths in core learning flows).
- **Technical base is clean** enough to trust for future self-use and eventual symbolic-price users.

*Note: “Perfect” and monetization are longer-term; this run should move clearly toward that without requiring production billing in scope unless user adds it.*

## Current Pain Points (user-stated)

- **Visual polish:** App does not look very clean.
- **Reliability:** Not all features work bug-free.
- **Localization:** Missing or wrong translations; overall unfinished product feel.
- **Scope:** No tabus for fixes — Railway/production OK if changes stay **secure** (no leaked secrets, no unsafe data handling).

## Constraints

**Off-limits (do NOT change):**
- Commit or log secrets (`.env`, API keys, tokens).
- Destructive data operations without explicit approval.
- Insecure patterns (SQL injection, XSS, exposed admin without auth, etc.).

**Must preserve:**
- Existing learning features that already work (levels, practice, localization) unless improving UX intentionally.
- Personal GitHub account (`linuspohlmann-byte`), not work/Enpal credentials.

**Dependencies/requirements:**
- `OPENAI_API_KEY`, DB settings per `docs/LOCAL_DEV.md` / `docs/CONFIGURATION.md`
- Railway for production (per `docs/DEPLOYMENT.md`)

## Verification Approach

Based on project type, verification should use:

- [x] Browser testing (web apps with UI) — primary for UX goals
- [ ] CLI testing
- [x] HTTP request testing (APIs) — secondary
- [ ] Test suite execution — TBD if pytest exists after explore
- [ ] Type checking — TBD
- [ ] Lint checking — TBD
- [x] Documentation review
- [x] Manual review for UX polish

**Dev server command:** `python3 run_local.py` (port 5001 per README)
**Test command:** None (no pytest); browser smoke tests in Phase 4
**Build command:** N/A (Python web app)
**Lint command:** None configured

## Emotional Context

**User's state:** Motivated hobby builder; quality-focused, future-oriented (self-use + symbolic pricing later).
**Communication style:** German; clear goals, product-minded.
**Notes:** Recent repo cleanup and GitHub sync completed; confidence in stated priorities: **high**.

## Run History

**Previous runs:** First ShipLoop run
**Last run result:** N/A
**Learnings from previous runs:** N/A
