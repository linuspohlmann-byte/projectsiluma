# Python: default Nixpacks install (pip). Node: build SPA in build phase.
providers = ["python", "node"]

[phases.build]
cmds = ["chmod +x scripts/build_frontend.sh", "bash scripts/build_frontend.sh"]
